SELECT IFNULL(
    (SELECT DISTINCT Salary
    FROM Employee
    ORDER BY Salary DESC
    LIMIT 1 OFFSET 1), NULL) AS SecondHighestSalary
​
-- IFNULL(expr, alter) if expr is Null, return alter. Otherwise, return expr.

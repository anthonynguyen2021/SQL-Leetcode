# Write your MySQL query statement below
UPDATE Salary
SET sex = IF(sex = 'm', 'f', 'm');
# IF statement has the form
# IF(expr, true_exp, false_exp) - if the expr holds, return true_exp, else false_exp.
​
